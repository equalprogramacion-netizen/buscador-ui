# Copia este archivo a `.env` y completa los valores

# --- Base de datos MySQL ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=railway
DB_TABLE=biotic_database

# --- Flask / App ---
FLASK_SECRET_KEY=dev-secret-key
ENV=development

# --- SSO Ligero (solo necesario en producción) ---
GATEWAY_SHARED_SECRET=dev-shared-secret
GATEWAY_SHARED_SECRET_PREV=
GATE_AUD=buscador
HUB_HOME=http://127.0.0.1:8000/choose
SVC_SESSION_COOKIE=svc_buscador
SVC_SESSION_TTL=1800
EXPORT_FOLDER=temp_exports
